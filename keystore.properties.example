# Copy this file to `keystore.properties` and fill in real values.
# DO NOT commit `keystore.properties` to git (already in .gitignore).
#
# Alternative: export the same keys as environment variables (CI use).

RELEASE_STORE_FILE=bizconnect-release.jks
RELEASE_STORE_PASSWORD=
RELEASE_KEY_ALIAS=bizconnect
RELEASE_KEY_PASSWORD=
